import GeneralBackground from '../components/GeneralBackground/GeneralBackground';

const Future = () => {
  const sections = [<p>we have a bright future</p>, <p>yay yay yay</p>];
  return <GeneralBackground sections={sections} />;
};

export default Future;
